export interface Inquiry {
  inquiry_id: string;
  location_id: string;
  rep_id: string;
  inquiry_date: string;
  inquiry_channel: string;
  status: string;
  inspection_required: boolean;
  notes: string;
}